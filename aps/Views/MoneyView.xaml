<UserControl x:Class="aps.Views.MoneyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:aps.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600"
             Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Movimentações" FontSize="20" FontWeight="SemiBold" Margin="20"/>

        <ListView Grid.Row="1" Margin="20" x:Name="listView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Id}" Width="20"/>
                    <GridViewColumn Header="Tipo" DisplayMemberBinding="{Binding Type}" Width="80"/>
                    <GridViewColumn Header="Descrição" DisplayMemberBinding="{Binding Description}" Width="270"/>
                    <GridViewColumn Header="Valor" DisplayMemberBinding="{Binding Value}" Width="50"/>
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="80"/>
                    <GridViewColumn Header="Data" DisplayMemberBinding="{Binding Date}" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>