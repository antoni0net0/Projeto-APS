<Window x:Class="aps.Views.AppWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:aps.Views"
        xmlns:userControls="clr-namespace:aps.Views.UserControls"
        mc:Ignorable="d"
        Title="AppWindow" Height="600" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Navigation bar -->
        <StackPanel Background="LightGray">
            <TextBlock Text="Nome da empresa" FontSize="20" HorizontalAlignment="Center" Margin="10"></TextBlock>
            <userControls:SidebarButton Height="50" Margin="10" Button_Content="Inicio"></userControls:SidebarButton>
            <userControls:SidebarButton Height="50" Margin="10" Button_Content="Movimentaçoes"></userControls:SidebarButton>
            <userControls:SidebarButton Height="50" Margin="10" Button_Content="Funcionários"></userControls:SidebarButton>
        </StackPanel>
    </Grid>
</Window>
